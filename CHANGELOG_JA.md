# 更新内容

Nexira の各リリースが、実際に使う人にとって何を意味するか。バージョン 2.4.1 から記録しています。英語の原文は [CHANGELOG_EN.md](./CHANGELOG_EN.md) にあります。

開発ログは別の文書です。[CHANGELOG.md](./CHANGELOG.md) はクラスや仕組みの名前を並べたもので、リリースの内容を知るために読む必要はありません。

日本語訳は母語話者によるものではありません。おかしな言い回しを見つけたら、課題トラッカーへ知らせてください。

## [Unreleased]

音楽プレイヤーが十種類になり、置く場所に合うものを選べるようになりました。ジャケットを主役にしたカード、再生中の盤の色を取り込むカード、ジャケットのない素のファイルを集めたフォルダ向けの簡素な表示、ポインタを乗せるまでジャケットしか映らないタイル、レコード盤、セル一つ分の大きさのトークン、そして曲そのものの形を軸にしたものがいくつか。これまでのプレイヤーはジャケット主体のカードに置き換わり、画面上の位置はそのままです。

そのうち四つは曲そのものを描きます。ランチャーがファイルを一度読んで形を求めるので、伸びていく棒ではなく、その曲の実際の輪郭が見えます。再生済みの部分は塗られます。処理はファイルあたり約一秒、一度だけ、背景で行われ、測れなかったファイルは穴を残さず平らなままになります。

複数のファイルをまとめて読み込むと、それが再生待ちの列になります。前後への移動、一曲または列全体の繰り返し、不要な項目の削除ができます。列と現在の位置は再起動しても残り、再生を押すまで何も開かれません。

デスクトップが再生内容を把握するようになりました。メディアキーが効き、メディアウィジェットに曲名、演奏者、ジャケットが表示され、そのボタンからランチャーを操作できます。システムの音量パネルには、名前のない Java のプロセスではなく Nexira として専用のアイコン付きで並ぶので、単独で音量を下げたり、別の出力へ移したり、エフェクトの規則を向けたりできます。

どの目盛りも押した位置に応え、ドラッグに追従します。棒でも、曲の形でも、それを縦にしたものでも、盤を囲む輪でも、カードの本体そのものでも同じです。

細かな修正も多くあります。目盛りへのクリックが半分ほどしか効かなかったのが、毎回効くようになりました。盤の周りのドラッグは、手が縁の外へ出ても追従を続けます。曲を切り替えたあと、シークが前の曲の長さのまま計算することがなくなり、短い曲の中ほどを押して末尾へ飛ぶことがなくなりました。時刻表示と棒の位置が食い違わなくなりました。タグのない曲は読み込み中に名前が変わらず、一つの名前のままです。ジャケットのカードの再生ボタンが暗い配色でも読めるようになりました。空のプレイヤーは、ジャケットの四角が描かれない幅でも、どこを押してもファイルを開きます。

サーバーに紐付いていないパックは、常にオフラインで起動するようになりました。以前は、サインイン中のアカウントのセッションが渡されることがあり、そのパックに含まれる Mod がそれを持ち出せました。プロフィールからのオフラインでのサインインが正しく動作し、再起動後も保持されるようになりました。

## [2.4.5] - 2026-09-15

遅れているパックが、プレイを押した時点で自ら追いつくようになりました。これまでランチャーは、ファイルがそのパックのものではないと気づいても画面には何も出さず、ログインを黙って渡さないまま、それでもゲームを起動していました。そのためサーバーには、理由がどこにも示されないまま入れませんでした。こうなるのは簡単でした。あるバージョンのランチャーがパックの一部の Mod を導入できず、次のバージョンが導入できるようになっても、古いファイルはそのまま残っていたからです。これからは足りない分を先に取得してから起動します。ミラーに届かない場合は、これまでとまったく同じように起動します。

CurseForge にある大きな Mod が、また導入できるようになりました。およそ 24 MB を超えるものは分割して取得しますが、CurseForge のエッジサーバーは、その一部を求める要求には「見つからない」と答える一方、同じファイルを丸ごと求めれば誰にでも渡します。ランチャーはこれを、ファイルが無いという意味に読んでいました。24 MB のライブラリが一つあるだけで、パック全体が未検証のままサーバーに拒まれていました。

これまでミラー自身が配信していたModが、作者が公開している場所から直接届くようになりました。パックの導入手順は変わりません。ファイルはこれまでどおり取得され、これまでどおり検証されます。どれがGitHub由来かはランチャーが表示します。

任意 Mod の切り替えが、一覧を開いている間に完了したパックの更新を、こっそり取り消してしまうことはなくなりました。また、切った Mod が、その時たまたまファイルを掴まれていたせいで読み込まれ続けることもなくなりました。ランチャーは取得の後にもう一度試し、それでも駄目なら、その旨を伝えます。

パックを以前のビルドへ戻すと、ファイルだけでなく、そこに何が含まれるかの一覧も元に戻ります。これが無いと、パックは次の起動で自分の Mod を失うことがありました。その一覧は、書き込みの途中でのクラッシュや停電でも失われなくなりました。

確認と修復を最後まで行えなかった場合は、問題なく終わった時と同じ緑の行を出さず、そのことを伝え、取得できなかったファイルの名前を挙げます。

## [2.4.4] - 2026-09-15

Mod が CurseForge にあるパックが、再びすべて導入されるようになりました。ランチャーは Modrinth とミラーからは取得できましたが、CurseForge で公開された Mod は黙って抜け落ちていました。そのためパックは改変扱いになり、ログインは行われず、サーバーにも入れませんでしたが、画面には理由が出ませんでした。これらの Mod も他と同じように取得されます。CurseForge の鍵は要りません。リンクはミラーがあらかじめ解決します。

作者が第三者による再配布を許可していない Mod は、別に扱われます。取得できる場所がないため、ランチャーはその旨をそのまま伝えます。更新しても解決しないからです。

パックの確認と修復は、取得できなかったものを名指しするようになりました。パックを丸ごと「問題なし」とは言いません。また、そうした Mod をご自分で置いた場合は、それが認められます。パックの記述と一致するファイルなら、あなたが置いたもので構いません。

一覧にこのバージョンでは解釈できない項目がひとつあっても、パックはその項目を除いて導入されます。これまでは、その一項目のために一覧全体が読めませんでした。

任意 Mod の選択は、CurseForge にある Mod でもパックの更新をまたいで保たれ、バージョンとともにファイル名が変わっても初期化されません。

CurseForge の Mod は、アイコンが見つかる場合に表示します。

## [2.4.3] - 2026-09-12

パックが、自分の Mod の書いたファイルのせいで改変扱いされることがなくなりました。ランチャーは mods フォルダの中身すべてを見ていましたが、これからはゲームが実際に読み込む二か所だけを見ます。ですから、Mod 自身のキャッシュや、Mod が自分で展開したライブラリはそのままにされます。同じ変更で逆向きの穴も塞がりました。jar ではなく、ただのフォルダとして置かれた Mod は、これまで一度も咎められずに読み込まれていましたが、これからは報告されます。

自分の使えるメモリを削り続けていたパックが、それを取り戻します。ランチャーは必要な量を以前のセッションから割り出しますが、起動から数秒で死んだ回も同じ一回として数えていたため、失敗が続くと値が下限まで下がって、そのまま戻りませんでした。これからは、ある程度続いたセッションだけを数えます。値はまた上がれます。

## [2.4.2] - 2026-09-12

1.7.10 のパックが、起動から数秒でセッションを自分で切ってしまうことがなくなりました。ゲームの起動中に、二つの Mod が他の Mod の隣へ jar を置きます。IndustrialCraft 2 は自身の中に抱えたライブラリを展開し、CodeChickenCore はパックにもともと入っていた Mod を自分のフォルダへ移します。どちらもパックが書き換えられたように見えていました。パック自身の jar が抱えていたファイルと、パックの Mod がただ移動しただけのファイルを、名前ではなく中身で見分けるようになりました。見慣れた名前に差し替えられたファイルは、これまでどおり止まります。

## [2.4.1] - 2026-09-12

インターフェースの言語に日本語が加わりました。実験的な扱いです。日本語を描ける書体もランチャーに同梱したので、日本語、中国語、韓国語の文字は、システムに何が入っているかに左右されず、ランチャー自身の書体で表示されます。インターフェースがどの言語であっても、曲名やパック名にも同じことが当てはまります。

いちばん手が入ったのは音楽プレイヤーです。形は一つではなく四つになりました。ジャケットを主役にしたもの、カードの色をジャケットから取るもの、画像を出さずに再生時間を主役にしたもの、そして再生の進みにあわせてカード自体が満ちていくものです。ファイルは一度に複数選べて、そのままキューに並びます。前後の曲送りと、一曲またはキュー全体の繰り返しも使えます。音量は起動のたびに最大へ戻らず、次に開いたときも保たれています。

メニューと選択リストは、隣に現れるのではなく、押した部品そのものから開きます。画面の端でもウィンドウの内側に収まり、下へはみ出す代わりにスクロールします。文字のないボタンは、ポインタを重ねると何をするのかを教えます。これができていたのは、これまでランチャー全体で四か所だけでした。音量のつまみは、すき間の空いた帯ではなく、またつまみらしい姿になりました。

ニュースのパネルは二つ目のチャンネルを出せます。ランチャーに同梱されていて設定のいらない読み物か、設定で指定した任意の RSS または Atom のアドレスです。このチャンネルの各行は文章だけです。どこも開かず、画像も取りに行かないので、チャンネル全体でも、指定した一つのアドレスへの通信一回で済みます。

何もしていないランチャーはメモリをシステムに返すようになりました。およそ 610 MB から 375 MB へ。上限はどれも下げていないので、負荷のかかる場面で使える余地は今までどおりです。

別の jar を内部に抱えた Mod を含むパックで、そのファイルが失われなくなりました。Scalar もその一つです。これまでは削除され、起動から数秒でセッションが切れていました。

Windows のインストーラーがまた作れるようになりました。ライブラリを先に置くホーム画面は取りやめました。ライブラリはもともとサイドバーから一回のクリックで開けたので、同じ画面への二本目の道になっていたためです。
